#!/bin/bash
pkg=cscrew-rxvt
action="$1"

function add_divert {
	diverted_file="$1.diverted-$pkg"
	if test "$action" = install
	then
		dpkg-divert --package "$pkg" --add --rename \
			--divert "$diverted_file" "$1"
	fi
}

add_divert "/etc/X11/app-defaults/URxvt"